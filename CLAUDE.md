# Panel Comercial — CLAUDE.md

> **Instrucción para Claude (aplica siempre en este repo):** este archivo es la fuente de estado vivo del proyecto. Cada vez que termines trabajo en algún archivo del repo (cambio funcional, fix, feature nueva, deploy pendiente que se resolvió, etc.), actualizá VOS MISMO la sección correspondiente en "Estado actual por archivo" (qué se hizo, qué queda pendiente) antes de cerrar el tema — sin que el usuario tenga que pedirlo. Si el cambio no encaja en ninguna sección existente, creá una. Mantené cada sección corta (3-6 líneas). No dupliques acá el detalle de arquitectura/URLs/IDs que ya está en `handoff.md` — solo linkealo.

## Qué es esto

Sebastian es auditor comercial en M&A Equipamientos Comerciales (Tucumán, Argentina). Repo público en GitHub Pages con dashboards HTML autocontenidos + backend en Google Apps Script. Detalle completo de arquitectura, URLs de deploy, IDs de Drive y config de sucursales: ver `handoff.md`.

## Estado actual por archivo

### reporte-comercial.html
- Dashboard de producción por zona. Ventas históricas ene-jun 2026 (~31k registros), carga XLS, merge sin duplicados.
- **Ya resuelto (verificado 2026-07-24):** el bloque `<script type="application/json" id="initial-data-embed">` (línea ~2485) ya tiene la estructura mínima vacía — el histórico de ~31k ventas ya NO está incrustado. Pendiente original de `handoff.md` cumplida, esa sección de `handoff.md` quedó desactualizada.
- Pestaña IA depende de `AIProxy.gs` (pendiente de deploy) + `AI_CONFIG.APPSSCRIPT_URL` (vacío hasta deployar).
- **Tabla Detallada (2026-07-24):** agregada fila de totales al final de `#tableBody` (`renderTable()`, función principal ~línea 4890+). Suma dinámica de columnas `neto, contado, financiado, reservas, ops, recibos, clientes` sobre las filas visibles actuales (respeta filtros + búsqueda + selección de columna/toggle M$-%). Columnas no sumables (rank, mix, share, ticket) quedan en blanco en esa fila. CSS: clase `.total-row` (sticky bottom, borde superior, sin hover/click).
- **Import nómina — fix (2026-07-24):** en el importador de Excel de la Nómina (`_importNominaExcelInput` change handler, ~línea 6592), al actualizar un vendedor existente ahora solo pisa los campos que vienen con dato en el archivo — antes `Object.assign` sobreescribía TODO, incluidos campos vacíos, borrando Zona/Cobrador/Lider/Sexo/Tipo equipo cargados a mano si el excel no traía esas columnas. Necesario para poder re-importar archivos parciales sin perder datos manuales.
- **Conexión con nomina-produccion.html (2026-07-24):** `nomina-produccion.html` tiene botón "Exportar p/ Reporte Comercial" que genera un `.xlsx` (Nro Vend./Vendedor/Sucursal/Fuerza de venta/Fecha Ingreso) a partir del roster clasificado (`roster`/`CAT_ORDER`). Se sube con el botón existente "Cargar Excel" de Tabla Detallada > Vendedores. Zona/Cobrador/Lider/Sexo/Tipo equipo NO vienen en ese export (no existen en nomina-produccion) — se completan a mano como siempre, y ahora no se pierden al re-exportar/re-importar (ver fix de arriba). Mapeo de categoría→Fuerza y de sucursal están en `CAT_TO_FUERZA`/`AREA_TO_SUCURSAL_REPORTE` dentro de `nomina-produccion.html`; "Líder CRM" no tiene clave propia en `FUERZA_OBJETIVOS` de reporte-comercial y cae al objetivo `_DEFAULT` (categoría muy chica, ver si hace falta agregarla).
- **Fix adicional descubierto probando la conexión (2026-07-24):** `getCol(row, 'FuerzadeventaFuerzaventa', 'Fuerza', 'FuerzaVenta')` en el importador de Excel de nómina (~línea 6619) tenía dos candidatos de columna pegados sin coma (`'FuerzadeventaFuerzaventa'`) — nunca matcheaba ningún header real, así que "Fuerza de venta" quedaba SIEMPRE vacío al importar cualquier excel (no solo el export nuevo). Corregido a `getCol(row, 'Fuerza de venta', 'Fuerza Venta', 'Fuerza', 'FuerzaVenta')`. Probado end-to-end en browser: 223 filas importadas, fuerza y sucursal correctos en todas.
- **Retheme Notion light+dark (2026-07-25):** último de los 9 archivos del panel en pasar al sistema de diseño Notion (`design-system/panel-comercial/MASTER.md`), en rama `rediseno-notion`. Se reescribió el bloque `:root`/`html[data-theme="dark"]` (mismos nombres de variable, valores nuevos), tipografía a Inter + IBM Plex Mono, y se hizo `Chart.defaults.color/borderColor` dinámicos vía helper `cssVar()`. De los 18 `new Chart(...)` del archivo solo se rethemearon ejes/grillas/datalabels de texto (antes hardcodeados en `#566579`/`#eef1f5`); las paletas categóricas de datos (Contado/Financiado en azul/naranja `#1e4fa3`/`#f78d2d`, estados de reserva, paleta de sucursales, etc.) se dejaron sin tocar a propósito, igual que en el resto de los archivos del panel — son datos, no chrome. Los tooltips de Chart.js (`#0b1220`) también quedan fijos siempre oscuros por diseño. Agregado el listener `storage`/`panelTheme` para sync en vivo con el toggle de `index.html`. Verificado en browser (light y dark) en las 9 pestañas, modal de detalle de plan, dropdowns de filtro, tabla con fila de totales y mapa Leaflet — sin errores de consola.

### control-agendas.html
- Lee 10 espacios de trabajo (Sheets por sucursal) vía `LectorAgendas.gs` (ya deployado, restringido a @myacomercial.com).
- Parser de fechas por regex `dd/mm`, robusto. Si aparece fecha rara, es dato mal cargado en el Sheet, no bug del parser.
- `gerente` en `CONFIG.SUCURSALES` acepta string o array de apellidos candidatos (ej. La Rioja: `["Rios","Nieto"]`) — evita falsos "líder" cuando no se sabe exacto cómo está escrito el apellido en la pestaña, o cambió la persona.
- Label "Semana X" ahora sale de la misma hoja que da los días mostrados (`ref`), no de la primera hoja con la palabra "semana" que se encontraba (podía ser de otra sucursal).
- Backup semanal (sábados) de los 10 Sheets vía `AgendaBackup.gs` — pendiente de deploy, ver abajo.
- **Selector de semanas históricas (2026-09-03, deployado y verificado):** backend nuevo `AgendaHistorial.gs` lee las copias de `Backups Agendas/Semana YYYY-MM-DD/` y expone `accion=semanas`/`accion=leer&semana=` con el mismo shape que `LectorAgendas.gs` (`{archivo,modificado,hojas}`, usa `getDisplayValues()` igual que el lector en vivo), para reusar `parseAgenda()` sin cambios. Frontend: `CONFIG.HISTORIAL_URL`, selector `#semanaSelect` en `.controls` (se oculta solo si no hay URL o no hay semanas — `pintarSemanaSelect()`), banner `#histBanner` en modo histórico con botón "Volver a la semana actual" (`volverAVivo()`). `state.modo`/`state.semanaHistorial` controlan todo: `irASemana()` trae el snapshot vía `aplicarSnapshotHistorico()` (arma `personas`/`sucursales` igual que `cargarTodo`, vía el helper compartido `personasDeSucursal()`). La pestaña Covente fuerza `volverAVivo()` al entrar (no tiene sentido con semanas pasadas).
  - **3 bugs reales encontrados y corregidos probando contra datos de producción (solo lectura) en servidor local:** (1) *Condición de carrera:* si el usuario saltaba a una semana histórica mientras la carga en vivo inicial todavía estaba en vuelo, esa carga en vivo terminaba después y pisaba los datos históricos sin avisar — guard `if(state.modo!=="vivo") return` al final de `cargarTodo`, y guard `if(state.semanaHistorial!==semana) return` en `irASemana` (por si el usuario cambia de semana o vuelve a vivo mientras esa fetch específica sigue en vuelo). (2) *Etiqueta de semana rota:* la barra de días y "Semana histórica X al Y" se armaban leyendo la fila de fechas de la primera hoja/persona encontrada en el snapshot (`p.dates`) — una hoja real tenía una columna basura fuera de la semana real y el label mostró "31/08 al 29/12". Fix: como ya se sabe exactamente qué sábado es (`state.semanaHistorial`), la función nueva `semanaDeSabado()` calcula lunes-a-sábado directo desde esa fecha (mismo patrón que `semanaRealActual()` pero anclado a una fecha conocida en vez de "hoy") — ya no depende del contenido de ninguna hoja. `aplicarSnapshotHistorico()` también usa esto para fijar `state.diaSel` (lunes de esa semana) en vez de la primera fecha cruda de una hoja cualquiera. (3) *404 intermitentes de Apps Script:* un Web App recién autorizado/deployado devuelve 404 sueltos en la misma URL durante los primeros minutos (confirmado en vivo: la misma URL alternaba 200/404 sin ningún cambio de código) — se agregó `fetchHistorialJSON()` (3 intentos, 600ms entre cada uno) usado tanto por `cargarSemanasDisponibles()` como por `irASemana()`; sin esto el selector quedaba oculto o la carga fallaba de pura mala suerte de timing, no por un error real.
  - Verificado en browser contra el backup real de la semana del 05/09 (10 sucursales, 61 personas): selector aparece, semana histórica carga con datos reales (agenda de Jujuy/Arjona con tareas y horarios correctos), banner y freshness (`act. dd/mm · hace X`) correctos, "Volver a la semana actual" funciona (puede tardar ~15s si Apps Script está bajo carga, normal).
  - Nota de deploy real: `AgendaHistorial.gs` necesita autorización OAuth propia la primera vez que se ejecuta como Web App (pantalla "Authorization needed" — hay que revisar permisos y permitir acceso a Drive), aunque sea el mismo proyecto/cuenta que ya corre `LectorAgendas.gs`/`AgendaBackup.gs`. Además, si `backupAgendasSemanal()` se corrió a mano con una cuenta distinta a la que ejecuta el Web App ("Ejecutar como"), los archivos copiados quedan sin permiso para esa cuenta (error nativo de Drive "No se puede abrir el archivo") — usar siempre la misma cuenta para todo este circuito.
- **Fix parseCovente (2026-08-05):** dos bugs reales, confirmados con el texto real de un informe de Fontana (Norte Interior). (1) Etiquetas Cantidad/Valor repetidas dentro del mismo informe (ej. dos bloques "Cantidad de Datos recopilados") pisaban el bloque anterior en vez de sumar — se perdía silenciosamente el primer bloque completo. Ahora `CV_CAMPOS_NUM` (línea ~808) suma cantidad/valor numéricamente y concatena el resto (detalle/condición) cuando la etiqueta se repite. (2) El label de observaciones solo matcheaba "Observaciones:" exacto; "Detalles y observaciones:" (variante real que usan los gerentes) no matcheaba y ese texto quedaba pegado dentro del campo Detalle anterior. Regex ampliado a `/^(?:detalles?\s*(?:y|e)?\s*)?observaciones\s*:?\s*(.*)$/i`. Verificado corriendo el parser real contra el texto pegado por el usuario antes y después del fix (node, no en browser).
- **Mensaje original colapsable + Resumen más ancho (2026-08-19):** `textoOriginal` de cada salida ya se guardaba en el registro (`guardarCovente`) pero nunca se mostraba — ahora `abrirCoventeDetalle()` lo agrega al final de la tarjeta en un `<details class="cv-raw">` nativo (colapsado por defecto, monoespaciado), sin JS de toggle. Modal de "Resumen de salidas" (`abrirCoventeResumen()`) ensanchado de 980px a `min(1400px,97vw)` — la tabla de 7 columnas con montos entre paréntesis no entraba y quedaba cortada/con scroll horizontal. Como el modal (`#modal`) se reutiliza entre todas las vistas Covente, las demás funciones que lo abren (`abrirCoventeDetalle`, `abrirCoventeForm`, `editarCovente`, `abrirModal` de agenda semanal) ahora resetean `style.width=""` explícitamente al abrir — si no, heredaban el ancho de 1400px si el usuario había abierto Resumen antes. Verificado en browser contra datos reales de producción (solo lectura): toggle con click real de mouse, tabla de Resumen sin scroll horizontal, otros modales vuelven a 980px.

### index.html
- Centro de Control / portal. Modo edición con `EDIT_KEY` (BackendPanel.gs): reordenar tarjetas, colores, ocultar, agregar custom. Persiste en `layout.json` (Drive).
- Apps se abren dentro de `<iframe id="contentFrame">` (SPA, no navegación real). Tarjetas tipo `"link"` (`TOOLS` array, o custom agregadas por el usuario) se renderizan como `<a href="archivo">` de verdad (2026-07-27) — click normal hace `preventDefault()` + carga en el iframe vía `abrirArchivo(key,archivo,nombre)` (que también pone `document.title = nombre + " · M&A"`, antes siempre quedaba "Centro de Control" sin importar qué app estaba abierta); Ctrl/Cmd/Shift+click, click de rueda y click derecho no se interceptan → el navegador los maneja nativo (pestaña/ventana nueva, menú contextual). Tarjetas `kb`/`graphics`/`gerencial` siguen siendo `<div>` (abren modales, no hay archivo que navegar).
- Cards especiales: "Base de Conocimiento" (docs Drive) y "Recursos Gráficos" (galería imágenes).
- **Base de Conocimiento fullscreen + fix de scroll (2026-07-28):** `abrirKB()` ahora activa `modal-wide`/`full` (ya existían, los usaba solo Gerente Regional) — modal a pantalla completa, el visor de imágenes/PDF (iframe de Drive) se ve mucho más grande. Bug real detrás de "no puedo bajar para ver todos los documentos": `renderKB()` pisaba `#kbBody.className` entero (`= kbView==="list"?"kb-list":""`), borrando la clase `modal-body` → nunca tuvo `flex:1`/`overflow:auto`, el contenido se cortaba silencioso contra el `overflow:hidden` del modal padre. Fix: conserva `modal-body` y agrega `kb-list` como modificador. De paso se agregó `min-height:0` a `.modal-body` en general (gotcha clásico de flexbox). `abrirGraphics()` resetea `className`/`full` al abrir para no heredar el fullscreen si KB quedó abierto antes; los cierres de KB y Recursos Gráficos usan `cerrarModalGerencial()` (limpia todas las clases) en vez de un handler propio.
- **Gerente Regional (módulo aparte, agregado 2026-07-23):** modal con pestañas tipo browser sobre una categoría propia de la Base de Conocimiento ("Gerente Regional") — manuales + notas por manual + notas sueltas + vista "Notas globales". Funciones `ger*` cerca de la línea 780+.
- **Notas ricas (2026-07-27):** las notas de Gerente Regional pasaron de `<textarea>` a `contenteditable`. Se puede pegar una imagen (Ctrl+V) — se achica por canvas (tope `GER_IMG_MAX_DIM=1100px`) y se inserta envuelta en `.ger-img-wrap`. El contenido se sigue guardando como string plano vía `kb_save` (sin cambios en `BackendPanel.gs`), ahora puede venir envuelto en `<div data-ger-rich="1">…</div>` con HTML+imágenes en base64 adentro; notas viejas (texto plano puro, sin ese wrapper) se siguen mostrando bien vía `gerContentToHtml()` (detecta el wrapper; si no está, escapa y convierte `\n`→`<br>`). `AIProxy.gs` limpia HTML/imágenes de estas notas (`limpiarNotaRica_()`) antes de mandarlas como contexto a la IA — **hay que redeployar ese script** para que tome el cambio.
- **Resize de imágenes pegadas (2026-07-27, reemplazado el mismo día):** el `resize:both` nativo del navegador no daba feedback visual y no era usable. Ahora es explícito: click en la imagen la selecciona (`.ger-img-wrap.selected`, outline celeste) y aparece un handle circular (`.ger-img-handle`) en la esquina inferior derecha; arrastrarlo cambia el tamaño manteniendo proporción, acotado al ancho del editor. Click afuera deselecciona (listener global en `gerWireDeselect()`, wireado una sola vez). Ya no usa `ResizeObserver` — el propio `mouseup` del drag dispara `onDirty()`. Sin handle ni selección en la vista de solo lectura "Notas globales" (`.gng-txt .ger-img-handle{display:none}`).
- **Fix handle invisible (2026-07-27, mismo día):** el handle se posicionaba con offset negativo (`right:-6px;bottom:-6px`, a propósito afuera de la caja) pero `.ger-img-wrap` tiene `overflow:hidden` (necesario para el clip redondeado de la imagen) — eso recortaba casi todo el handle, dejándolo prácticamente imposible de clickear (reportado por el usuario: "la primera vez sí, después no", en realidad pasaba siempre, en todas las notas). Ahora el handle va **adentro** de la caja (`right:3px;bottom:3px`) con `box-shadow` propio para distinguirse — no tocar `overflow:hidden` del wrap, sigue haciendo falta para el border-radius de la imagen. Si se vuelve a mover el handle, verificar que sus coordenadas no queden fuera del `getBoundingClientRect()` del wrap (eso es lo que lo recorta).
- **⚠️ Seguridad (detectado 2026-07-27, no tocado):** hay una pantalla de login (`#panelAuthOverlay`, agregada por otra sesión el 2026-07-25/26 junto al rediseño) con usuario y contraseña **hardcodeados en texto plano en el código fuente público** (`if (u === 'wsenieva' && p === '...')`, cerca de la línea 370). Cualquiera con "Ver código fuente" los lee. No da seguridad real y expone una contraseña que podría estar reusada en otro lado. Sebastian está al tanto, pendiente de decidir qué hacer (sacarla, o si es solo un filtro cosmético está bien pero conviene que la contraseña ahí no sea una que use en otro sistema).
- **⚠️ Seguridad #2 (detectado 2026-07-28, no tocado):** `CONFIG.EDIT_KEY` también quedó hardcodeado en texto plano (`"MAeditor2026"`, línea ~18) — el plan original de `handoff.md` era que Sebastian la definiera él mismo y NO estuviera en el código fuente (repo público). Mismo tipo de problema que el login de arriba, probablemente de la misma sesión del rediseño. No lo tocué, es una decisión de Sebastian si la saca o la deja.
- **Compartir notas por link público + exportar PDF/Word (2026-07-28):** botón 🔗 en ambos editores de notas (`renderGerDoc`/`renderGerNote`) guarda la nota (si no tenía `noteId`, la crea) y copia `nota-publica.html?id=<noteId>` al portapapeles vía `gerCompartirNota()`. `nota-publica.html` es un archivo nuevo, standalone, **sin la pantalla de login** (a propósito — el pedido era que se lea sin loguearse) — lee el mismo `kb_get` público de `BackendPanel.gs` que ya usa `index.html`, reimplementa localmente la lógica de `gerContentToHtml` (no depende del resto de la app). Botones PDF/Word junto al de compartir: `gerExportPDF()` (CDN `html2pdf.js`) y `gerExportWord()` (CDN `html-docx-js`, genera `.docx` real vía OOXML) — ambos limpian el HTML con `gerCleanExportHtml()` antes (saca el handle de resize y la clase `.selected`, que son solo del editor). Si se toca el editor de notas de nuevo, cualquier cambio a `gerContentToHtml()`/al formato de `data-ger-rich` en `index.html` hay que reflejarlo también en `nota-publica.html` (están duplicados a propósito, para que la página pública no dependa de cargar toda la app).
- **Layout del modal (2026-07-27):** se sacó el `.modal-head` propio de este modal (icono+título+X) para ganar alto y ver el documento completo en el visor de manuales — el botón de cerrar ahora vive en la barra de pestañas (`.gx-close`), a la derecha, fuera del `.gtabbar-scroll` (así no desaparece si hay muchas pestañas abiertas y hace scroll horizontal).
- **Split documento/notas (2026-07-27, revertido el mismo día):** se probó un divisor arrastrable entre el iframe y el panel de notas; el usuario pidió sacarlo y dejar fijo 50/50 (`.gsplit iframe{flex:1}` / `.gnotes{flex:1}`). No hay handle ni persistencia de ancho — si se vuelve a pedir resize, no reusar ese código viejo tal cual, el usuario ya lo probó y no lo quiso.
- **Pestaña fija "Inicio" (2026-07-27):** a la izquierda de "Notas globales", `state.gerActive="home"` → `renderGerHome()`. Misma grilla que "Nueva pestaña" (factorizada en `gerManualesGridHtml()`), pero clickear un manual/nota abre o activa una pestaña real aparte (`abrirGerDoc()` / `abrirGerNotaSuelta()`) en vez de transformarse ella misma en documento — sigue disponible siempre como "volver al principio".

### nomina-produccion.html
- Dashboard de headcount de producción por sucursal/categoría (NO es una ficha de legajos completa). Plano base actualizado (2026-07-24) con los 424 empleados del xlsx `listado empleados.xlsx` provisto por el usuario → 223 clasificados como producción.
- **Clasificación reescrita:** ahora usa `classifyEmployee(tarea, tipodven)` — prioriza la columna `tipodven` del xlsx (viene pre-clasificada de origen y separa producción/no-producción sin ambigüedad, validado 1:1 contra los datos reales); si el archivo cargado no trae esa columna (formato viejo de 7 columnas), cae a `TAREA_ALIAS` por texto de `tarea` como antes.
- **Categoría nueva:** "Líder Redes" (`tipodven=LIDER POR REDES` / `tarea=LIDER REDES`, 2 personas), agregada en `CAT_ORDER`/`CAT_COLOR`/`CAT_SHORT`/`TAREA_ALIAS` + var CSS `--c-liderredes`.
- Carga de xlsx (botón "Cargar plano") reconoce automáticamente `tipodven` si está en los headers (`COLS_OPT`), sin romper compatibilidad con planos viejos que no la tengan.
- Header muestra "Actualizado dd/mm/yyyy" junto al logo (fijo en el HTML para el plano base; se actualiza solo a la fecha del día al cargar un xlsx nuevo).
- Barra de sucursales (`.tabs-wrap`) con scrollbar horizontal visible — antes estaba oculta (`scrollbar-width:none`) y sucursales fuera del viewport parecían inaccesibles.
- **Regla de negocio:** "vendedores de terreno" = Vendedor Senior + Vendedor Junior (`TERRENO_CATS`). Se muestra como "(N terreno)" junto al número de Vendedor Senior en la vista Global, recalculado en vivo.
- **Pendiente:** reflejar en la app la info de la hoja de Drive `1gijnFhygEnvKz7pYp81cJCIgWAa5hIGrRGl_1zyxrfk` — bloqueado hasta que el usuario comparta acceso con `sebastianpereznieva@gmail.com` (la cuenta de Drive conectada no tenía permiso).
- **Export a Reporte Comercial (2026-07-24):** botón "Exportar p/ Reporte Comercial" genera `.xlsx` (Nro Vend./Vendedor/Sucursal/Fuerza de venta/Fecha Ingreso) desde el roster clasificado, compatible con el importador de Tabla Detallada > Vendedores de `reporte-comercial.html`. Ver detalle en la sección de `reporte-comercial.html` arriba.
- **Fix encoding (2026-07-24):** legajo 300 "ROIG BA¥A" → "ROIG BAÑA" en `RAW_DEFAULT` (la `Ñ` se había corrompido a `¥`, artefacto de codepage del xlsx origen). Único caso encontrado en todo el array (verificado con búsqueda de `¥` y `�` en la data, sin otros hits).
- **Plano base reemplazado (2026-09-02):** `RAW_DEFAULT` reconstruido desde cero a partir de `vendedores.xlsx` (216 empleados de producción, provisto por el usuario, formato distinto al de `listado empleados.xlsx` de julio — columnas `nombre/apellido/documento/empresa/area/fecha_alta/tarea/cod_ven/legajo/telefono`, sin `tipodven` ni `ingreso`). Clasificación cae 100% a `TAREA_ALIAS` (fallback por texto de tarea) ya que este archivo no trae la columna `tipodven`. Mismo bug de encoding que julio reapareció en el mismo legajo (300, "ROIG BA�A") — corregido a mano de nuevo a "ROIG BAÑA" antes de embeber.
- **Categoría nueva "Vendedor Redes Jr" (2026-09-02):** el xlsx trae 2 personas con tarea `VENDEDOR REDES JR`, sin equivalente en el dashboard. A pedido del usuario se creó como categoría propia (`CAT_ORDER`/`CAT_COLOR`/`CAT_SHORT`/`TAREA_ALIAS`, var CSS `--c-redesjr`) en vez de sumarlas directo a Vendedor Redes, PERO además se agrupan: junto al número de Vendedor Redes ahora se muestra "(N redes total)" = Redes + Redes Jr sumados (`REDES_CATS`, mismo patrón que `TERRENO_CATS` que ya sumaba Senior+Junior junto al número de Senior). Es decir, se puede ver la categoría separada (columna propia en la matriz/roster) y agrupada (el paréntesis) al mismo tiempo — mismo criterio que "terreno". En `CAT_TO_FUERZA` (export a reporte-comercial) "Vendedor Redes Jr" cae a la misma clave `VENDEDOR REDES` que Vendedor Redes (no tiene fuerza propia ahí, igual que pasa con Líder CRM → `_DEFAULT`).
- Verificado en browser (servidor local, login bypasseado seteando `panelAuthOK` en localStorage — no se tipeó la contraseña real): 216 total, 11 sucursales, 11 categorías, columna/paréntesis de Redes Jr correctos, tabla de detalle por sucursal ok, sin errores de consola.
- **Cards de categoría también en vista por sucursal (2026-09-02):** antes el grid de cards (`.cat-grid`, con valor + base + agrupado terreno/redes) solo existía en la pestaña Global; cada sucursal mostraba directo el detalle editable (`cat-card` con roster + pill +/-), sin el resumen visual de arriba. A pedido del usuario, se extrajo el grid a `statsGridHtml()` (antes generado inline solo en `renderGlobal`) y se agregó también en `renderArea()`, entre el legend y el detalle — misma tarjeta, mismos ids (`data-statval`/`data-statdelta`/`data-terrenoval`/`data-redesval`), sin colisión porque `#main` siempre tiene un solo panel activo a la vez (global o una sucursal, nunca ambos en el DOM). `updateAllNumbers()` se refactorizó: el bloque que pisa esas cards ahora corre siempre (antes solo dentro del `if(activeTab==='__global__')`), usando `valFor(cat)`/`baseFor(cat)` que resuelven a `catTotal`/`catBaseTotal` (global) o `state[area][cat]`/`base[area][cat]` (sucursal) según `activeTab` — el resto de cada rama (hero+matriz global / chip+pills de sucursal) no cambió. Verificado en browser (texto de página, no screenshot — la extensión tuvo timeouts de captura no relacionados al cambio): pestaña Salta muestra el grid con "(14 terreno)"/"(4 redes total)" agrupados y el detalle debajo sin tocar; +/- de un pill actualiza el card de arriba en vivo (probado con Vendedor Senior 10→11, terreno 14→15).
- **Importador de vendedores.xlsx (recurrente) + edición en vivo + persistencia en Drive (2026-09-03):** Sebastian va a subir siempre el mismo `vendedores.xlsx` (columnas `nombre/apellido/documento/empresa/area/fecha_alta/tarea/cod_ven/legajo/telefono`, sin `ingreso` propio ni `tipodven`) a medida que cambie la nómina real, y necesitaba tres cosas nuevas.
  - **Fix real del importador (botón "Cargar plano"), 2 bugs encontrados probando contra el archivo real (no contra datos ya transcriptos a mano como el 2026-09-02):** (1) `ingreso` estaba en `COLS` (requerida) — sin esa columna, el import completo abortaba con "No reconocí las columnas (faltó: ingreso)". Se movió a `COLS_OPT` (igual que `tipodven`) y si falta, `ingreso` toma el mismo valor que `alta` (pedido explícito de Sebastian: "la fecha de alta se puede tomar como fecha de ingreso"). (2) Con eso arreglado, las fechas salían como "1/9/20" (sin cero a la izquierda, año de 2 dígitos, orden M/D/AA) en vez de "09/01/2020" — causa: `sheet_to_json(...,{raw:false,cellDates:true})` fuerza el string preformateado de Excel (ignora `cellDates`), y ese formato corto es ambiguo/específico de la config regional con la que se generó el xlsx. Con `raw:true` SheetJS entrega el objeto Date real y `excelDate()` (ya existía, sin cambios) lo formatea bien. Esto también arreglaba en silencio el export a Reporte Comercial: `ingresoToISO()` exige año de 4 dígitos vía regex — con el string corto ambiguo, "Fecha Ingreso" salía SIEMPRE vacía en el .xlsx exportado; ahora sale bien en el 100% de los 217 registros probados.
  - **Edición en vivo por persona:** cada fila de la tabla de detalle tiene ✎ (editar: apellido, nombre, sucursal, categoría — reasigna al toque, splice del bucket viejo al nuevo vía `buildFromRecs()`) y 🗑 (dar de baja: `activo=false`, sale de todos los conteos pero no se borra — aparece en una tarjeta colapsable "Dados de baja" al final de cada sucursal, con botón para reactivar). Refactor de base: `buildFromRaw()` (usada solo al importar un xlsx, corre `classifyEmployee`) ahora es un wrapper de `buildFromRecs()` (recalcula `AREAS`/`base`/`roster`/`state` a partir de una lista de registros YA clasificados — la usan tanto el import como la carga desde el backend y cada edición). `allRecs` (nueva variable global) es la única fuente de verdad completa, incluye inactivos; todo lo demás se deriva filtrando `activo!==false`.
  - **Persistencia en Drive vía `NominaBackend.gs` (nuevo, mismo patrón que `AgendaHistorial.gs`/`CloudSync.gs` — pendiente de deploy):** `CONFIG.APPSSCRIPT_URL` (vacío por defecto) + `CONFIG.EDIT_KEY` (`"MAeditor2026"`, mismo valor que el resto del panel) al lado de `RAW_DEFAULT`. Sin URL configurada el panel funciona exactamente igual que antes (arranca de `RAW_DEFAULT`, nada se guarda, indicador de guardado oculto). Con la URL puesta: `cargarInicial()` reemplaza el viejo `buildFromRaw(RAW_DEFAULT); render();` del arranque — lee `accion=leer`, y si el backend ya tiene datos los usa (si no, cae a `RAW_DEFAULT` igual que antes); cada edición de persona y cada import de un xlsx nuevo llama a `guardarRoster()` (debounce 400ms, guarda `allRecs` completo + `fileLabel`). Decisión de Sebastian sobre conflictos: **el excel siempre gana** — importar un xlsx nuevo pisa por completo cualquier edición manual previa (no hay merge por legajo); las ediciones en vivo solo sirven para corregir/ajustar la foto actual entre una importación y la siguiente, no sobreviven a la próxima carga de archivo a propósito.
  - Pendiente del usuario: deployar `NominaBackend.gs` (pasos en el propio archivo, mismo criterio que `AgendaHistorial.gs`: Ejecutar como "Yo", acceso "Cualquier usuario de myacomercial.com") y pegar la URL en `CONFIG.APPSSCRIPT_URL`. Sin eso, todo lo demás (import corregido, edición en vivo) funciona igual pero solo dura mientras la pestaña esté abierta.
  - Verificado en browser (servidor local, login bypasseado, contra el `vendedores.xlsx` real de Sebastian, sin backend configurado — esa parte no se pudo probar end-to-end porque requiere el deploy manual): import da 217 empleados/11 sucursales, 0 fechas mal formateadas, 0 "Fecha Ingreso" vacías en el export; mover una persona de sucursal por la UI (click real en ✎, no solo JS) actualiza conteos de origen y destino al instante y persiste en `allRecs`; dar de baja/reactivar (`confirm()` mockeado para el test) resta/suma del total sin borrar a la persona; modal revisado en claro y oscuro.

### cotizador-comparativo-v3.html / planes-vigentes-tarjetas.html / circuito-ventas-entes-publicos-caso2-mercaderia-remitida.html / proyeccion-gerentes-dashboard.html / informe-showroom-sucursales.html
- **Retheme Notion light+dark (2026-07-25):** mismo trabajo que en `reporte-comercial.html` (ver arriba) — sync vía `localStorage.panelTheme` + `storage` event. Sin cambios funcionales, solo capa visual (CSS + config de color de Chart.js). En `cotizador-comparativo-v3.html` se preservaron los tokens semánticos de estado de precio (`--suba/--baja/--alta/--disc/--igual`); en `circuito-ventas-entes-publicos-caso2-mercaderia-remitida.html` los 5 colores de etapa; en `informe-showroom-sucursales.html` los charts dentro de `.slide` quedan con colores fijos a propósito porque ese div es preview de una placa exportada a PNG (no debe seguir el tema del visor).

### evolucion-vendedores.html
- **Retheme Notion light+dark (2026-07-25):** mismo trabajo, con la particularidad de que el editor (chrome) y el slide exportable (`#theSlide`) usan namespaces de variables CSS separados a propósito — el slide mantiene sus colores/tipografía fijos (`Segoe UI`) para que el PNG exportado no varíe según el tema del que lo genera.
- **Carga de JSON del Reporte Comercial (2026-08-06):** antes los datos (`const D` — vendors/fuerzas/sucursales/daily/daily_suc/dateRange/months) venían hardcodeados, pegados a mano como un blob JSON gigante en el `<script>` (única forma de actualizar el gráfico era regenerar y pegar ese blob de nuevo). Ahora hay un botón "📥 Cargar JSON del Reporte Comercial" (panel superior, `#jsonFileInput`) que acepta el backup completo que exporta `reporte-comercial.html` (`dashboard_backup_*.json`, campo `ventas`) y reconstruye `D` en el cliente: `rebuildD()` agrupa por vendedor/sucursal/fecha (`[total,contado,financiado]` según `tipo_venta`, NC ya vienen con `neto` negativo así que no hace falta tratarlas aparte), `aplicarNuevaData()` reemplaza `D` in-place (sigue siendo `const`, se limpia con `Object.keys(D).forEach(k=>delete D[k])` + `Object.assign`) y resetea selección/chips/dropdowns de año-mes que antes se armaban una sola vez al cargar la página. Sucursal/fuerza de cada vendedor se toma de su venta más reciente (puede haber cambiado de sucursal en el período). Verificado en browser con una muestra real de 2000 filas: monto agregado por vendedor exacto contra la suma manual, sin errores de consola. `D.vendors`/`fuerzas`/`sucursales` embebidos que quedan en el archivo son ahora solo el estado inicial por defecto — se pisan apenas se carga un JSON nuevo.
- **Fix escala de montos (2026-08-10):** `fmtLabel()` mostraba "bill." (billón anglosajón = 10^9) para montos ≥ mil millones, redondeando a 1 decimal — perdía precisión y confundía (el billón argentino es 10^12). Ahora todo monto ≥ 1e6 se muestra en millones con separador de miles es-AR (ej. `$ 1.238 mill.` en vez de `$ 1.2 bill.`).
- **Editar montos de un slide ya generado (2026-08-10):** antes la única forma de corregir un monto era crear un colaborador manual desde cero (`+ Manual`). Ahora cada slide (colaborador/sucursal/equipo) tiene botón "✎ Editar montos" (junto a Descargar/Copiar JPG) que abre modal con Neto/Contado/Financiado por período, precargados con el valor calculado — reusa `parseAmt`/`fmtManualAmt` ya existentes del flujo Manual. El ajuste se guarda en `overrides` (objeto global, key `slideKey|dateFrom|dateTo`) vía `getPeriodVals()`, que intercepta todo punto donde se leen montos (chart, tarjetas de resumen normal y de equipo) — persiste al navegar entre slides y al volver a tocar "Generar" con la misma selección; "restablecer" lo saca. Se limpia solo al cargar un JSON nuevo (`aplicarNuevaData`). Verificado en browser: valores prellenados exactos, override refleja en chart/resumen, persiste tras regenerar, reset funciona.
- **Fix checkbox ZIP + solape de etiquetas del gráfico (2026-08-10):** el checkbox `.thumb-chk` de cada slide en la lista inferior no tenía `onclick` — `toggleZipExclude()` existía pero nunca se llamaba, no se podía deseleccionar nada para exportar solo lo relevante (bug real, reportado por el usuario). Wireado a `toggleZipExclude(i,event)`. Aparte, los 3 datalabels (Neto/Contado/Financiado) usaban el mismo `align:'top'`/offset — cuando dos valores coinciden o quedan cerca (típico: financiado=0 → neto=contado exacto) los textos se dibujaban superpuestos e ilegibles. Ahora Neto va `align:'top'` offset 10 y Contado/Financiado van `align:'bottom'` con offset distinto entre sí (4 y 16) — quedan en franjas separadas aunque los puntos coincidan en pixel; `layout.padding.bottom` subido a 34 para que no se corten. Nuevo selector "Peso de las etiquetas" (Normal 600/Semibold 700/Bold 800 por defecto/Extra bold 900) en el panel, aplica en vivo vía `labelWeight` + `setLabelWeight()`. Color de Neto Total oscurecido a `#0C447C` (navy, mismo valor que el token `--bd` del slide) para distinguirse de Contado (`#0b7fc2`) cuando quedan cerca — antes ambos eran variantes de azul muy parecidas. Verificado en browser con el peor caso real (financiado=0) y con clicks simulados en el checkbox.
- **Selector de tamaño + fix choque contra eje X (2026-08-10, mismo día):** el fix de arriba dejaba un caso sin cubrir, reportado con captura real: cuando un valor chico (ej. Financiado "$118 k") queda comprimido cerca del piso del gráfico porque la escala está dominada por otro período mucho más grande en la misma serie de datos (ej. "$7 mill." en el punto siguiente), el offset fijo hacia abajo lo empujaba contra la fecha del eje X ("Mar 2026"), ilegible. Primer intento (saltar la etiqueta al lado opuesto) generaba un choque nuevo contra la etiqueta de Neto — descartado. Solución final: el offset de la serie secundaria "baja" (la de menor valor absoluto entre Contado/Financiado en ese punto) se recorta al espacio real disponible hasta el eje, medido en pixels vía `context.chart.scales.y.getPixelForValue()` / `context.chart.chartArea` — nunca lo cruza, se queda cerca de su propio punto en vez de saltar. Ver función `secOffset()` dentro de `render()`. Nuevo selector "Tamaño de las etiquetas" (Chica 9/Normal 11 por defecto/Grande 13/Extra grande 15) junto al de peso, mismo patrón (`labelSize` + `setLabelSize()`). Verificado en browser reproduciendo el caso exacto de la captura del usuario.
- **Editar texto de un slide ya generado (2026-08-10, mismo día):** mismo patrón que "Editar montos" — botón "✎ Editar texto" (verde, `right:378px` en la barra del viewport, a la izquierda de Editar montos) abre modal con Nombre/Subtítulo/Etiqueta del encabezado (`sName`/`sMeta`/`sBadge`), precargados con el valor actual (`v.label`/`v.sub`/`v.badge`). Se guarda en `textOverrides` (key: `slideKey`, sin período — es texto del slide entero, no por período) y `render()` lo aplica sobre los 3 `textContent`. Persiste al navegar y al regenerar igual que los overrides de montos; se limpia solo en `aplicarNuevaData`. El nombre de archivo de descarga/ZIP ya leía `sName.textContent` directamente (no `v.label`), así que automáticamente usa el nombre editado sin cambios adicionales. Verificado en browser: prellenado exacto, no afecta otros slides, persiste tras navegar/regenerar, reset funciona, botones sin solaparse.
- **Equipos personalizados: sumar cualquier combinación de colaboradores en 1 sola placa (2026-09-02):** el modo "Equipo consolidado" ya existente solo arma equipos automáticos por fuerza de venta (1 slide por fuerza). El usuario pedía algo distinto: elegir a mano un grupo arbitrario (ej. 5 vendedores + su líder, de categorías distintas) y sumarlos en una única placa editable. Se agregó una fila nueva "Sumar varios colaboradores como equipo" (modo `vendedor`, buscador propio `teamSearchInput`/`teamDdList`, independiente del buscador que arma slides individuales) — se van agregando nombres a `teamPick` (Set, chips con × para sacar uno), y el botón "+ Sumar N seleccionados como equipo" (`commitTeamPick()`, pide nombre con `prompt()`) lo convierte en una entrada de `customTeams` (array persistente: `{id,label,members}`). `buildSlides()` empuja un slide `{type:'team', custom:true, members:[...]}` por cada entrada de `customTeams`, reutilizando 100% la maquinaria ya existente para equipos (`sumPeriodTeam`, thumbnail con lápiz de edición, "✎ Editar texto" vía `textOverrides` — nada de eso hubo que tocar). Único fix necesario: `openModal()` (el editor "Editar equipo" del lápiz) armaba el universo de candidatos filtrando `D.vendors` por `v.f===sl.label` — funciona para equipos automáticos (label = nombre de la fuerza) pero para un equipo personalizado el label es un nombre libre que no matchea ninguna fuerza real, así que el editor quedaba con 0 candidatos. Ahora chequea `sl.custom`: si es `true`, el universo es toda la nómina (`D.vendors.slice()`, sin filtrar por fuerza ni sucursal) — así se puede agregar/sacar cualquier persona, incluida gente de otra fuerza (el "líder"), después de creado el equipo. `clearAll()` also limpia `customTeams`/`teamPick`. Verificado en browser: suma de 3 colaboradores vs. cálculo manual (`sumPeriod` por separado) exacta; agregar un 4to integrante de otra fuerza vía el lápiz funciona; "Editar texto" renombra el equipo sin tocar el resto; flujo de búsqueda probado con clicks reales de mouse (no solo JS).
- **Fix etiquetas del gráfico: recorte en los bordes y solape en cruces de líneas (2026-09-02):** dos bugs reportados con captura real del gráfico "Empresa" (3 series: Neto/Contado/Financiado, `render(idx)` ~línea 1600). (1) El primer y último período mostraban la etiqueta cortada en el borde del canvas (ej. el "$" de "$ 4.726 mill." o el "mill." final desaparecían) — `layout.padding.left/right` (8px/14px) no alcanzaba para la mitad del texto que sobresale del punto extremo. Fix: `clamp:true` en los datalabels de las 3 series (`dlNeto`/`dlSec`) — la etiqueta se empuja hacia adentro del área del gráfico en vez de recortarse — más padding left/right subido a 36px como colchón visual. (2) Cuando Contado y Financiado tienen valores muy parecidos en un punto (cruce de las dos líneas, ej. $2.370 vs $2.283 mill.), la curva de una línea (con `tension:.35`, puede "pandear" más allá de los dos puntos que une) pasaba justo por encima del texto de la otra etiqueta y la tapaba — no era solape de las dos etiquetas entre sí (`secOffset()` ya las separaba razonablemente) sino la línea tapando el texto. Fix: halo blanco semitransparente detrás de cada etiqueta (`backgroundColor:'rgba(255,255,255,.82)'` + `borderRadius`/`padding`, objeto `labelBg` reusado en las 3 series) — mantiene el número legible sin importar qué pase por detrás (línea, grilla, relleno de área), en vez de intentar calcular con precisión por dónde pasa cada curva. De paso, `secOffset()` ahora también mide la distancia real en pixels entre los dos puntos (Contado/Financiado) en cada punto del eje y, si están muy pegados, estira un poco más el offset de la etiqueta "perdedora" (menor valor absoluto) para separarlas mejor. Aplica a los 3 modos (`vend`/`suc`/`team`/`empresa` comparten el mismo `render()`). Verificado en browser inyectando valores de prueba vía el mecanismo de `overrides` ya existente (mismos números que la captura del usuario, no había datos reales cargados en la sesión de prueba): bordes izquierdo/derecho ya no se cortan, etiquetas de Contado/Financiado en el cruce legibles con el halo.
- **Fix Neto/etiquetas cuando una forma de pago cierra en negativo (2026-09-02, reportado con Rojas Cristian Antonio):** dos bugs relacionados, ambos en el mismo `render(idx)`. (1) `fmtLabel()` comparaba `val>=1e6`/`val>=1e3` directo — con un valor negativo (ej. Financiado -1.636.758 por devoluciones) esa comparación siempre daba falso y caía al último `return`, mostrando el número crudo sin redondear ni sufijo ("$ -1636758" en vez de "$ -2 mill."). Fix: compara por `Math.abs(val)` y deja el signo puesto en el cálculo (`Math.round` ya lo respeta) — misma lógica que los positivos, ahora también para negativos (`-23871` → "$ -24 k", `-1636758` → "$ -2 mill."). (2) Un valor negativo también hundía la línea muy por debajo de cero, dejando la etiqueta (anclada "abajo" del punto) sin espacio y superpuesta con las fechas del eje X. Fix: se separaron los valores "reales" (para el texto) de los valores "para dibujar" (`netoPlot`/`contPlot`/`finPlot` en vez de pisar directo `netoData` etc.) — un valor negativo se pisa a un piso chico y positivo (6% del máximo positivo del gráfico, `negFloor`) solo para la posición del punto/línea; el número mostrado en la etiqueta sigue siendo el real vía `formatter:(v,ctx)=>fmtLabel(realArr[ctx.dataIndex])` en `dlNeto`/`dlSec`. Bug adicional encontrado probando: `secOffset()` decide qué serie (Contado/Financiado) "gana" y quién se aleja más según magnitud real, pero la fórmula de separación asumía que la "ganadora" siempre queda más arriba en pantalla que la "perdedora" — con el piso eso se puede invertir (la ganadora por magnitud real termina pisada más abajo que la perdedora, que sigue en su valor real positivo) y el cálculo daba un offset chico en vez de grande, tapando la etiqueta de la ganadora. Corregido usando la diferencia de pixels CON SIGNO entre los dos puntos en vez de `Math.abs`. Verificado en browser con Rojas Cristian Antonio (Mar/Abr/May, Abr con Financiado y Neto negativos vía `overrides`): las 3 etiquetas (Neto, Contado, Financiado) legibles y separadas, línea apenas arriba de la base en el mes negativo, sin regresión en el caso sin negativos (re-verificado el cruce Contado/Financiado del fix anterior).
- **Fix sucursal/fuerza de vendedor mal asignada por un registro suelto (2026-09-03, reportado con Mamani José Luis Alberto):** `rebuildD()` calculaba la sucursal/fuerza "actual" de cada vendedor tomando la de su **venta puntual más reciente** — un único registro mal cargado en el origen (ej. día equivocado con sucursal equivocada) pisaba el dato de meses de historial correcto. Caso real: Mamani tenía 275 ventas cargadas como Salta y 2 cargadas por error como Norte Interior el último día con datos (28/08) — esas 2 ganaban por ser las más recientes, mostrándolo como Norte Interior en todo el panel (en `reporte-comercial.html` no pasaba porque ese archivo usa un padrón fijo importado a mano, no lo recalcula por venta). Fix: ahora se toma la sucursal/fuerza **más frecuente entre las ventas del vendedor en su último mes de actividad** (`vendorMonthSuc`/`vendorMonthFuerza`, función `mode()`) — sigue reflejando un traspaso real de sucursal (alcanza con que la mayoría de ese último mes ya esté en la nueva) pero ya no se deja "pegar" por uno o dos registros sueltos. Con `vm.s`/`vm.f` (la venta más reciente) como fallback solo si el último mes no tiene datos de sucursal/fuerza. Verificado corriendo la función exacta del archivo en Node contra `ventas-backup.json` real (44.043 ventas, 587 vendedores, 61ms): Mamani → Salta correcto; auditoría de impacto completa (comparando resultado viejo vs nuevo para los 587) mostró otros 27 vendedores con el mismo problema de fondo, corregidos de paso — la mayoría casos claros (ej. 20 votos contra 1), un par de empates cercanos (ej. 1 contra 1) donde el resultado exacto es menos determinante pero no peor que antes. **Nota de performance, no relacionada a este fix:** probando la carga completa del backup real (19MB) en browser, `aplicarNuevaData()` (código preexistente, reconstruye toda la UI después de cargar) se cuelga o tarda muchísimo a esa escala — no se investigó a fondo, puede valer la pena revisar si se nota lento/trabado el botón "Cargar JSON"/"Cargar desde backup" en uso real.
- **Fix Neto tapado por Contado cuando Financiado se va negativo (2026-09-03, reportado con captura real de Rodriguez Jorge Humberto):** la etiqueta de Neto (`dlNeto`) siempre iba con offset fijo hacia arriba, sin mirar a Contado/Financiado — funciona bien mientras Neto es la serie más alta (caso normal), pero si Financiado se va muy negativo en un punto (ej. Contado 24M + Financiado -3M = Neto 21M), Neto puede terminar por DEBAJO de Contado ahí — su etiqueta, que sigue "subiendo" desde un punto ahora más bajo, terminaba pisando la etiqueta de Contado (ej. "$27 mill." tapado detrás de "$24 mill." en Ago 2026). Fix: nueva función `topOffset()` (mismo criterio de pixels con signo que `secOffset()`, pero mirando hacia arriba) — el offset de Neto ahora también se estira cuando su punto queda a la par o por debajo del de Contado o Financiado, tomando el máximo requerido contra las dos. `dlNeto` pasó a recibir `otherPlots=[contPlot,finPlot]` además del color/array real. Verificado en browser reproduciendo el caso exacto (Contado 24M/Financiado -3M/Neto 21M vía `overrides`): "$ 21 mill." y "$ 24 mill." quedan claramente separados aunque el punto de Neto esté más abajo, sin regresión en el caso normal (Neto arriba de todo, ej. Mar 47M/40M).
- **Prefijo por puesto en nombres de archivo exportados (2026-09-04):** pedido del usuario — al ordenar alfabéticamente los JPG/ZIP descargados, quería que los vendedores del mismo puesto (fuerza de venta) quedaran agrupados entre sí. Nuevas `sanitizeFN()`/`currentFilePrefix()` (esta última lee `#sBadge`, que ya refleja `textOverrides` si el usuario editó el texto del slide) usadas en las 3 vías de exportación (`downloadSlide`, `downloadAllZip`, fallback de `copySlide`): el nombre pasa a `PUESTO_NombreColaborador_DDMMYYYY.png` (ej. `VENDEDOR_REDES_Monzon_Magali_Alejandra_04092026.png`). Sin puesto cargado, el prefijo cae a `SINPUESTO` en vez de quedar vacío. Slides de sucursal/equipo/empresa usan su propio badge (`SUCURSAL`/`EQUIPO`/`EMPRESA`) como prefijo, mismo criterio. Verificado en browser contra datos reales (467 vendedores) sin depender de `html2canvas` (el slide no se renderiza a tamaño real fuera del flujo completo de "Generar", así que se probó la construcción del string directamente, incluido el fallback `SINPUESTO`).
- **Selección múltiple de meses con orden de click = orden de generación (2026-09-04):** antes "Períodos a comparar" solo permitía agregar un período a la vez (Año/Mes/Día + "+ Agregar"), machacoso para armar comparativas de varios meses salteados. Se agregó una fila de chips (`#monthMultiChips`, un chip por mes del año elegido en `pYear`, se regenera en cada cambio de año vía `updateMonthOptions()`) — clickear un chip lo suma a `monthPickOrder` (array `{y,m}`, en orden de click, no cronológico) y le pinta un badge numerado con su posición; volver a clickearlo lo saca y renumera el resto. Botón "+ Agregar N seleccionados" (`addPickedMonths()`) los empuja a `periods` respetando ese orden exacto (que es el que después usa `render()` para las columnas P1/P2/P3... del gráfico) — omite duplicados ya presentes en `periods` y corta al llegar a 6 períodos, avisando en ambos casos. `monthPickOrder` se resetea en `clearAll()` y en `aplicarNuevaData()` (mismo patrón que `periods`/`customTeams`). El selector clásico de un período a la vez queda intacto (sigue siendo la única vía para año completo o rango de días dentro de un mes). Verificado disparando clicks reales sobre el DOM (`el.click()`, no solo llamando las funciones JS) contra datos reales: orden Mar→Ene→May generó `periods` en exactamente ese orden (no cronológico), duplicado omitido con aviso, límite de 6 respetado.

### generador-placas-ia.html (nuevo, 2026-09-10)
- Genera placas publicitarias e imágenes de producto con fondo vía Gemini ("Nano Banana", modelo `gemini-2.5-flash-image`), a partir de una foto de producto (subida directo o elegida de una carpeta de Drive) + un prompt editable. Basado en los dos prompts que venía usando Sebastian a mano (placa con precios/cuotas y foto de producto limpia sin texto).
- **Dos pestañas independientes**, cada una con su propio prompt base editable (placeholders `{{FORMATO}}`/`{{FONDO}}`/`{{VISTA}}`/etc., se completan solos al generar): "Placa con precios" (headline, subheadline, código, cuotas, precio final, legal — logo de la empresa se adjunta solo, leyendo `assets/logo.png`) y "Producto con fondo" (foto de catálogo limpia, sin gráfica ni textos).
- Selectores de formato (1:1 feed / 16:9 horizontal / 9:16 historias, exclusivos, mandan `aspectRatio` a Gemini), fondo y vista (chips que rellenan un campo de texto libre editable, no exclusivos). Campo "Variantes" se agrega al final del prompt sin pisar el resto.
- Selección múltiple de imágenes (Drive y/o subidas locales) + cola de generación: se arma con "Agregar seleccionadas a la cola" y se puede correr toda junta ("Generar toda la cola", secuencial con 1.2s de espera entre llamadas y barra de progreso) o de a un trabajo suelto (botón "Generar" por ítem) — ambos modos conviven, a pedido de Sebastian.
- **Backend nuevo `NanoBananaProxy.gs` (pendiente de deploy, ver abajo)** — mismo patrón que `AIProxy.gs`: la API key de Gemini vive en Propiedades del Script (repo público, no puede ir en el HTML), y una `IA_ACCESS_KEY` separada que el HTML pide una vez y guarda en `localStorage` (`nanoBananaKey`). `doGet(action=listar)` lista las imágenes de `PRODUCTOS_FOLDER_ID` (hay que pegar el ID de la carpeta real en el .gs) devolviendo miniaturas vía `file.getThumbnail()` (liviano, no baja la imagen completa para listar). `doPost(action=generar)` arma el `generateContent` de Gemini con la imagen (de Drive por `fileId`, o subida suelta por `imagenB64`) + el logo opcional + el prompt, y devuelve la imagen generada en base64.
- **Estado real (2026-09-10, verificado en browser):** backend deployado, `PRODUCTOS_FOLDER_ID`/`GEMINI_API_KEY`/`IA_ACCESS_KEY` cargados, `CONFIG.APPSSCRIPT_URL` pegado. Clave de acceso, subida local de imagen, cola y llamada real a Gemini probadas end-to-end — la única traba real pendiente es que el proyecto de Google Cloud de esa `GEMINI_API_KEY` **no tiene facturación activa** (Gemini devuelve `403 PERMISSION_DENIED`, no es bug del código). Sebastian tiene que activar billing en aistudio.google.com/apikey para poder generar imágenes de verdad.
- **Pestaña "Personalizado" (2026-09-10):** tercera pestaña además de Placa/Producto, mismo mecanismo de selectores pero con `promptBase` en blanco — para armar un prompt libre desde cero reusando fondo/vista/formato/variantes. `cfg.custom` en el objeto `cfg`, sin campos de placa.
- **Editor de texto + stickers sobre el resultado (2026-09-10):** botón "✎ Editar texto" en cada tarjeta de resultado abre un modal (`#editorBg`) con capas arrastrables/redimensionables sobre la imagen ya generada — texto (fuente **Gotham Pro**, los 9 pesos reales de la marca embebidos vía `@font-face` desde `assets/fonts/*.OTF|TTF`, color con paleta de marca `COLOR_PALETTE` de 25 tonos extraída de `Colores y tipografía/COLORES M&A.png`) y **stickers** (logo M&A en 9 variantes + ~20 logos de tarjetas/bancos/cuotas, registro `STICKERS` en el `<script>` de config, archivos en `assets/stickers/` + `assets/tarjetas/`). Los stickers redimensionan con proporción bloqueada (`iniciarResizeSticker`, calcula `hPct` a partir de `wPct`/`aspect` cada vez que se arrastra el handle) — no se pueden deformar. "Aplicar sobre el resultado" quema las capas a un canvas al tamaño real de la imagen (`flattenEditor`, usa `document.fonts.ready` antes de medir/dibujar texto) y reemplaza `job.resultadoB64`; "Descargar PNG" exporta sin tocar el resultado guardado. Probado en browser con una imagen sintética (agregar texto, agregar sticker Visa, resize con aspecto bloqueado confirmado por cálculo, aplicar y descargar) — sin errores de consola.
- **Presets de prompts — deshabilitado por ahora (2026-09-10):** se había agregado UI + backend (`NanoBananaProxy.gs` → `presets_listar`/`presets_guardar`, guarda un `nanobanana-presets.json` en la misma carpeta de Drive) para guardar/cargar variantes de prompt modificadas, compartido entre computadoras. Al probar en real: `doPost` responde bien, pero `DriveApp.Folder.createFile` tira `No tienes permiso... Permisos necesarios: .../auth/drive` — el proyecto de Apps Script nunca terminó de autorizar el scope de Drive para esta función nueva, y ejecutar `listarProductos` a mano desde el editor para forzar el prompt de autorización quedó colgado sin terminar. A pedido de Sebastian, la barra de presets se ocultó (`style="display:none"` en `.presetbar`, `generador-placas-ia.html`) en vez de borrar el código — sigue todo ahí (front y back) para retomarlo el día que se resuelva la autorización de Drive.

### comparativa-gerentes.html (nuevo, 2026-09-14)
- Adaptado de un archivo entregado por el usuario (`comparativa-gerentes (1).html`, Downloads) que comparaba facturación mes actual vs. mes anterior por gerente (tabla + 6 gráficos SVG), con carga manual de un `dashboard_backup_*.json` por dropzone.
- **Carga automática desde `ventas-backup.json`** (mismo patrón que `proyeccion-gerentes-dashboard.html`): se sacó el dropzone/FileReader y ahora `cargarBackup()` hace `fetch('ventas-backup.json', {cache:'no-store'})` al entrar a la página, con botón "↻ Actualizar" para reintentar sin recargar. Shape del backup (`{ventas,reservas,exportado}`, cada venta con `fuerza_venta/vendedor/fecha/neto/tipo_venta/sucursal_venta`) coincide 1:1 con lo que ya esperaba `procesar()`, así que la lógica de agregación/tabla/gráficos no se tocó. Pill "Datos al dd/mm/yyyy" con `data.exportado`, igual que el resto del panel.
- Se agregó el mismo overlay de login (`#panelAuthOverlay`, hardcodeado, ver nota de seguridad de `index.html`) que tienen el resto de los htmls standalone del repo, y logo con link a `index.html` — no traía ninguno de los dos en el archivo original del usuario.
- Agregada tarjeta en `index.html` (`TOOLS`, icono 📊) apuntando a este archivo.
- No probado en browser contra el backup real (solo revisión de código + inspección del shape de `ventas-backup.json` con Node) — antes de dar por cerrado conviene abrirlo una vez y confirmar que carga y filtra bien.
- **Copiar tarjetas KPI como PNG (2026-09-14):** a pedido del usuario, cada tarjeta `.kpi` (Facturado Neto/Contado/Financiado/% Objetivo Equipo) tiene un botón `.kpi-copy` (ícono clipboard, visible en hover, esquina superior derecha) que renderiza esa tarjeta sola con `html2canvas` (`ignoreElements` saca el botón mismo de la captura) y copia el PNG al portapapeles vía `navigator.clipboard.write([new ClipboardItem(...)])` — mismo patrón que `copySlide()` de `evolucion-vendedores.html`. Si el navegador no soporta `ClipboardItem` (o falla el permiso), cae a descarga directa del PNG. Mismo mecanismo replicado en `proyeccion-gerentes-dashboard.html` (que ya cargaba `html2canvas` para el export general) — ahí el HTML de las 4 tarjetas es estático, no generado por JS, así que el botón está hardcodeado en cada una. No probado clic real en browser (solo validación de sintaxis JS con Node); antes de dar por cerrado conviene probar el copiado en las dos apps y confirmar que el paste final da un PNG limpio sin el ícono del botón.
- **Copiar la tarjeta de gráfico completa (2026-09-14, mismo día):** a pedido del usuario, también se puede copiar como PNG la tarjeta grande de gráfico (no solo los KPI). En `proyeccion-gerentes-dashboard.html` es el `.evol-card` de "Evolutivo de facturación" (botón flotante arriba a la derecha, mismo `.kpi-copy`/`copiarKpiPng()` reusado tal cual). En `comparativa-gerentes.html` es la card `#chartsCard` ("Gráficos"): botón "📋 Copiar imagen" en la barra de herramientas junto a "Descargar gráfico PNG" — como esa card sí tiene una barra de botones visible (a diferencia de las tarjetas KPI), se le agregó la clase `no-capture` a `.tools` y el `ignoreElements` de `copiarKpiPng()` ahora excluye tanto `.kpi-copy` como `.no-capture` para que los botones no salgan en la imagen copiada. No se tocó la card "Meta global de facturación" de `proyeccion-gerentes-dashboard.html` ni la tabla de gerentes de ninguno de los dos archivos — el pedido fue puntual sobre las cards de gráfico.

### control-vacaciones.html (nuevo, 2026-09-21)
- Calendario de vacaciones por sucursal y por fuerza de venta, con detección de solapes configurable. Pedido del usuario: vista tipo Gantt (timeline anual con barras por persona), períodos de **15 días corridos que solo pueden arrancar un lunes** (si ese lunes es feriado, el inicio efectivo corre al próximo día hábil vía `siguienteHabil()`/`calcularPeriodo()`), carga por Excel de formato variable + carga/edición manual, y un log de auditoría de todos los cambios.
- **Reglas de solape (combinación pedida por el usuario):** (1) máximo configurable de personas de la misma fuerza de venta + misma sucursal de vacaciones a la vez (`reglas.maxPorFuerza`, con un default global y excepciones puntuales por sucursal/fuerza — editable en el modal "Reglas"), y (2) pares/grupos de personas específicas que nunca pueden coincidir (`reglas.excluyentes`). `calcularConflictos()` corre un sweep de eventos por grupo sucursal+fuerza para lo primero, y chequeo de solape de fechas por par para lo segundo; las barras en conflicto se marcan con outline rojo (`.tl-bar.conflict`) y tooltip con el detalle. El usuario avisó que va a seguir afinando los parámetros de esta regla con el uso — no está cerrada a propósito.
- **Nómina/fuerza de venta:** el usuario no quería cargar el mismo excel dos veces (una en `nomina-produccion.html`, otra acá). `CONFIG.NOMINA_URL` (2026-09-22) apunta a la MISMA URL de `NominaBackend.gs` que ya tiene pegada `nomina-produccion.html` — con eso puesto, `sincronizarNomina()` trae `allRecs` directo de ese backend (`?accion=leer`) y hace upsert al roster acá (match por legajo, si no por nombre normalizado) sin pisar personas cargadas localmente que no vengan de esa nómina (ej. las que entraron solas al importar un excel de vacaciones). Se llama sola y en silencio al abrir la página (`init()`), y también hay botón "🔄 Sincronizar nómina" para forzarla a mano. El excel manual (botón "📥 Cargar Excel nómina", export de "Exportar p/ Reporte Comercial" de `nomina-produccion.html`) se dejó como fallback para cuando no hay backend deployado — mismos `SUCURSAL_ALIAS`/`FUERZA_ALIAS` para los dos caminos. Edición manual del roster sigue disponible en el modal "Nómina" para corregir cualquier campo a mano en cualquiera de los dos casos.
  - **Bug real encontrado probando esto en browser (2026-09-22):** la sync silenciosa del `init()` llamaba a `registrar()`, que llamaba a `usuarioActual()`, que dispara `prompt()` nativo si `localStorage.vacUsuario` todavía no está seteado — un `prompt()` bloquea el hilo de JS por completo, así que la página quedaba congelada (probado con mock de `fetch`: la pestaña del navegador dejó de responder a cualquier cosa, hubo que abrir una pestaña nueva). Fix: `registrar(accion,detalle,automatico)` — con `automatico=true` usa `localStorage.vacUsuario` si existe o cae a `"Sistema (auto)"` sin preguntar nunca; `sincronizarNomina(manual)` pasa `automatico=!manual`, así que el sync silencioso del arranque nunca puede colgar la página, y el botón manual sigue pidiendo el nombre la primera vez como cualquier otra acción del usuario. Re-probado con mock de `fetch` (altas, actualización por legajo sin duplicar, 0 cambios detectados correctamente) — sin prompt, sin colgarse, log muestra "Sistema (auto)" en el camino silencioso.
- **Import de vacaciones (formato libre):** `findCol()` intenta reconocer variantes de headers (Nombre/Apellido y Nombre/Vendedor, Fecha Inicio/Desde/Inicio Vacaciones, etc.); si una persona del excel no matchea a nadie de la nómina ya cargada, se crea sola con sucursal/fuerza vacías y queda en el panel "Pendientes de asignar" hasta que se completen a mano. Si la fecha de inicio importada no cae lunes, el período entra igual pero queda marcado `estado:"revisar"` (barra con textura rayada) en vez de recalcularle la fecha sola — la regla de negocio es estricta y una corrección automática podía tapar un dato mal cargado en origen.
- **Planos reales de RRHH (2026-09-22, probado con 3 archivos reales del usuario: xlsx Catamarca + 2 PDF de Tucumán Interior/general) — reescritura grande del importador:**
  - Los planos reales combinan nómina+vacaciones en una sola tabla (`ccosto/area/legajo/apellidos/nombres/tarea/fecha_alta/Días Vacac./obs./inicio/[reintegro]/pte.`). "Días Vacac." es el total ANUAL que le corresponde a la persona (14/21/28/35 según antigüedad, no la duración del período) y "pte." son los días que le quedan pendientes después de este período (a veces con nota tipo `"7 p/julio"`, parseado por `parsePte()` en número+nota). Se agregaron `diasEntitlement`/`diasPendientes` al objeto período (se muestran en el tooltip de la barra y en el modal de edición si vinieron de un import). Verificado con cálculo real de día de semana (Node) que el >95% de los "inicio" de estos planos caen lunes — confirma que la regla de negocio de 15 días corridos/solo lunes que ya tenía el motor de fechas es correcta, no hubo que tocarla.
  - `tarea` en estos planos incluye roles fuera de fuerza de venta (Administrativo de Área, Encargado de Depósito, Líder Operativo, Maestranza, Oficial de Crédito, Supervisor, Maestro Especial) — se agregaron a `FUERZAS`/`FUERZA_ALIAS`/`FUERZA_COLOR`. `SUCURSAL_ALIAS` sumó las abreviaturas reales vistas (`"TUC. INT."`, `"N. CENTRO"`). `norm()` ahora también saca puntos y barras (antes solo acentos) para que matcheen sin importar cómo vengan escritas.
  - **Soporte de PDF (nuevo):** `extraerFilasPDF()` (pdf.js, CDN) reconstruye filas/celdas por posición (X/Y) de cada fragmento de texto — ancla cada celda a la columna del header más cercana en X (con tolerancia dinámica = 30% del espacio mínimo entre columnas vecinas, no un píxel fijo) para que una celda vacía no corra todo lo siguiente una posición. Si la tabla venía partida en 2 páginas por ser muy ancha (visto en un plano real), se detecta porque la página siguiente tiene la MISMA cantidad de filas que la primera y se pegan lado a lado en vez de agregarse como filas nuevas. `extraerFilasDeArchivo()` decide xlsx vs pdf por extensión y ambos caminos devuelven el mismo formato de filas — el resto del importador no sabe de dónde vino el archivo. Si el PDF es una imagen/captura sin texto real (probado con un archivo real del usuario), `extraerFilasPDF()` tira error explicando exactamente eso en vez de fallar silencioso — OCR queda fuera de alcance por ahora.
  - **3 bugs reales encontrados probando contra los 3 archivos reales (no mocks):** (1) el sentinel interno `nombreCombo=-2` (modo "apellido+nombre en columnas separadas") se chequeaba con `<0`, así que `-2` caía en la rama de error y el import fallaba siempre que el archivo traía "apellidos"/"nombres" en vez de una columna "Nombre" combinada — reescrito a un booleano explícito en los dos importadores (nómina y vacaciones). (2) La reconstrucción de celdas de PDF intentaba "pegar" fragmentos de texto cercanos en X asumiendo que pdf.js no separa bien las celdas — al revés: pdf.js YA entrega un ítem por celda, y mi propio merge por cercanía terminaba pegando nombre+tarea sin espacio cuando el ancho reportado no era preciso (confirmado con coordenadas reales). Se sacó ese merge. (3) El fallback que separa "tarea" de una celda de nombre pegada (`separarNombreDeTarea()`, para cuando ese merge de puntos anteriores igual pasa por geometría rara del PDF) armaba bien el valor pero el ternario que lo usaba (`ci.fuerza>=0 ? ... : tareaSuelta`) solo miraba si la COLUMNA existía, no si la CELDA venía vacía — nunca llegaba a usar el fallback. Los tres bugs se encontraron y corrigieron iterando en browser real contra los 3 archivos (no localStorage de prueba — se sirvieron desde una carpeta temporal `_testfiles/` borrada al terminar, con `CONFIG.APPSSCRIPT_URL`/`NOMINA_URL` vacíos a propósito para no escribir datos de prueba en el backend real de producción). Resultado final: Catamarca (xlsx) 22/22 personas y períodos correctos, con la misma inconsistencia real (un inicio en martes) que ya existía en el archivo; Concepción (pdf con texto real, 2 páginas) 21 personas/22 períodos (1 duplicado real en el origen) con sucursal/fuerza/fechas/entitlement 100% correctos y las mismas 2 fechas raras (sábado/domingo en vez de lunes) que ya tenía el archivo real, correctamente marcadas "revisar" en vez de aceptadas a ciegas.
- **Feriados:** lista editable en el panel (modal "Feriados"), sembrada con el calendario nacional 2026 conocido al momento de crear el archivo (`FERIADOS_SEED` en el bloque de configuración) — los feriados trasladables/por decreto cambian año a año y no se pueden calcular con una fórmula fija, así que quedan a cargo del usuario mantenerlos.
- **Historial de cambios:** cada alta/edición/baja de período, persona o regla llama a `registrar()`, que pide el nombre de quien está usando el panel una sola vez (`localStorage.vacUsuario`, no hay login individual) y lo guarda en `state.log` — visible en el modal "Historial".
- **Persistencia — deployado y verificado (2026-09-22):** `VacacionesBackend.gs` y `NominaBackend.gs` deployados por Sebastian, URLs pegadas en `CONFIG.APPSSCRIPT_URL`/`CONFIG.NOMINA_URL` de `control-vacaciones.html` (y `CONFIG.APPSSCRIPT_URL` de `nomina-produccion.html`). Probado end-to-end contra los backends reales (no mocks): `NominaBackend.gs` devuelve los 217 vendedores reales; `sincronizarNomina()` los trae y arma el roster correcto (10 sucursales, categorías bien mapeadas); `VacacionesBackend.gs` persiste el roster sincronizado en Drive y sobrevive a un reload de la página (confirmado con `localStorage` limpio, la segunda carga trae los datos del backend, no de `RAW_DEFAULT`). Sin `CONFIG.APPSSCRIPT_URL`/`NOMINA_URL`, el panel sigue funcionando igual que antes (localStorage / carga manual de excel).
- Tarjeta agregada en `index.html` (`TOOLS`, icono 🏖️).
- **Pendiente real:** no se cargó ningún período de vacaciones de prueba contra el backend real (para no ensuciar el archivo de producción en Drive) — el flujo de alta/edición/eliminación de período y la detección de conflictos están probados a fondo pero contra un `fetch` simulado (sesión del 2026-09-21), no contra `VacacionesBackend.gs` real. Conviene que Sebastian cargue el primer período real desde la UI para cerrar la verificación end-to-end de esa parte.
- **Fix scroll horizontal del timeline + vista "Gerente + Líder" (2026-09-22):** el usuario reportó que no podía ver los 12 meses ni desplazarse — bug real: `.tl` no tenía ancho propio, se achicaba por porcentaje para entrar en la pantalla (sin scroll posible). Fix: `.tl` pasa a tener `min-width:1900px` adentro de un `.tl-scroll{overflow-x:auto}` nuevo — como todo el layout interno (`offsetPct()`, barras, gridlines, header de meses) ya usaba porcentajes relativos a `.tl`, no hubo que tocar el JS, solo el CSS. `.tl-label` (columna de nombre) pasa a `position:sticky;left:0` para que no se pierda de vista al scrollear. Verificado en browser: `scrollWidth` (1900px) > `clientWidth` (~1600px, confirma que hay contenido real para desplazar), `scrollLeft` programático mueve el header/barras y la columna de nombre se queda fija — un wheel event sintético (`dispatchEvent`) no dispara scroll nativo por ser simulado por la herramienta de automatización, pero es comportamiento estándar de cualquier `overflow-x:auto` con trackpad/scrollbar real, no requiere JS propio.
  - **Nueva pestaña "🤝 Gerente + Líder":** pedido del usuario — Gerente de Área y Líder Comercial se cubren entre sus vacaciones, y en la vista "Por sucursal" (que ahora incluye todo el personal, no solo fuerza de venta) quedaban perdidos entre el resto del staff. Vista nueva agrupada por sucursal (mismo criterio que "Por sucursal") pero filtrada a esas dos categorías únicamente, con Gerente primero y Líder Comercial después dentro de cada sucursal — así se ve de un vistazo si se van a solapar. Muestra igual las sucursales sin nadie cargado en esas dos categorías (revela sucursales sin líder comercial cargado). No crea ninguna regla de solape nueva — es solo una vista, las reglas de excluyentes/máximo por fuerza siguen siendo las del modal "Reglas" (si se quiere que Gerente+Líder de la misma sucursal nunca coincidan, hay que cargar esa restricción a mano ahí, como cualquier otro par).
- **Borrar nómina + backup de la nómina en GitHub (2026-09-22):**
  - Modal "Nómina" tiene botón "🗑️ Borrar toda la nómina" (`confirm()`, deja los períodos ya cargados intactos pero sin sucursal/fuerza asociada hasta re-sincronizar — mismo trade-off que ya existía al borrar una persona sola). Logueado en el historial (`borrar_nomina`).
  - **`nomina-produccion.html` → GitHub, mismo patrón que `CloudSync.gs`/`ventas-backup.json`:** `NominaBackend.gs` suma `accion=backup_now` (`backupToGithub_()`, copia casi 1:1 de `backupToGithub()` de `CloudSync.gs` — mismas Propiedades del script `GITHUB_TOKEN`/`GITHUB_REPO`/`GITHUB_PATH` [default `nomina-backup.json`]/`GITHUB_BRANCH`, cada Apps Script project tiene sus propias Propiedades así que no choca aunque `CloudSync.gs` esté en otro proyecto). Botón nuevo "☁️ Backup a GitHub" en `nomina-produccion.html` junto a "Exportar p/ Reporte Comercial", llama `?accion=backup_now`.
  - **`control-vacaciones.html` lee ese backup directo** (`cargarBackupNominaGithub()`, `fetch('nomina-backup.json')` relativo — mismo mecanismo que usan `comparativa-gerentes.html`/`proyeccion-gerentes-dashboard.html` con `ventas-backup.json`, sin pasar por ningún backend). Botón "📦 Backup GitHub" en la barra + auto-fetch silencioso en `init()` (junto al `sincronizarNomina()` en vivo existente — los dos hacen upsert, no se pisan entre sí, conviven). Se extrajo el upsert compartido a `aplicarRecsNomina(recs)` para no duplicar la lógica de match-por-legajo/nombre entre el camino en vivo (Apps Script) y el de backup (JSON estático).
  - Verificado en browser: `nomina-backup.json` de prueba servido localmente (no comprometido al repo) → `cargarBackupNominaGithub()` upsertea bien (2 nuevas, sucursal/fuerza correctas); "Borrar toda la nómina" con `window.confirm` mockeado (necesario para no colgar la sesión de automatización con el diálogo nativo) → roster a 0, log registrado. `backup_now`/botón de `nomina-produccion.html` no probados end-to-end todavía (necesitan `GITHUB_TOKEN` configurado en las Propiedades del proyecto de `NominaBackend.gs`, que Sebastian tiene que generar y pegar — mismos pasos que ya hizo para `CloudSync.gs`).
- **Bug real: conteo de empleados mal al cargar `empleados_produccion_22sept.xlsx` (2026-09-22, reportado por el usuario):** en los dos importadores de excel de `control-vacaciones.html` (nómina y vacaciones), `nombreCombo` (modo "arma el nombre desde apellido+nombreSolo") solo se activaba si NO existía una columna que matcheara como "nombre". El archivo real trae `apellido` y `nombre` en columnas separadas, pero la columna de nombre se llama literalmente `"nombre"` (no `"Nombres"` plural) — matcheaba como columna de "nombre completo", así que el importador tomaba SOLO el nombre de pila (ej. "LEONARDO DAMIAN") y tiraba el apellido ("GOMEZ"). Con nombres de pila cortos/repetidos, el matching difuso de `buscarPersonaPorNombre()` (substring) fusionaba personas distintas entre sí — 218 filas reales importaban solo 204 personas (14 fusiones falsas), confirmado probando el archivo real en browser. Fix: si existe columna de apellido, SIEMPRE se arma apellido+nombre por separado (usa "Nombres" si existe esa columna propia, si no cae a la que haya matcheado como "nombre" — en la práctica es el nombre de pila en archivos así) — ya no importa si también había una columna que parecía "nombre completo". Re-verificado en browser con el archivo real (218/218, nombres completos con apellido, 0 fusiones falsas) y sin regresión contra el excel de Catamarca ya probado antes (sigue 22/22).
- **Sucursal CRM + Nómina agrupada con resumen + borrar vacaciones por sucursal/persona (2026-09-22, pedido del usuario):**
  - `SUCURSALES` suma `"CRM"` como una sucursal más (con alias `"crm"` en `SUCURSAL_ALIAS`) — antes esos empleados (área `CRM` en los planos reales, ~6 personas) no matcheaban ninguna sucursal canónica y quedaban en el balde "Sin sucursal asignada" de todas las vistas. Ahora tienen su propio grupo, igual que cualquier otra sucursal, en "Por sucursal" y en el modal "Nómina".
  - **Modal "Nómina" reescrito:** ya no es una tabla plana — se agrupa por sucursal (`claveSucursalNomina()`/`idsDeGrupoNomina()`, mismo criterio "__sin__" que ya usaba `renderVista()` para la vista por sucursal) con un resumen de chips arriba (cantidad de personas por sucursal) y, en el header de cada grupo, la cantidad de personas + de períodos de vacaciones cargados en esa sucursal. Cada persona en la tabla muestra cuántos períodos tiene entre paréntesis (`periodosDePersona()`).
  - **Borrar vacaciones de una persona:** ícono 🧹 nuevo por fila (junto al 🗑️ que ya sacaba a la persona de la nómina — son acciones distintas a propósito, con íconos distintos para no confundirlas). `confirm()` + filtra `state.periodos` por `personaId`, logueado (`borrar_vacaciones_persona`).
  - **Borrar vacaciones de una sucursal completa:** botón "🗑️ Vacaciones (N)" en el header de cada grupo (solo aparece si ese grupo tiene algún período cargado). Mismo patrón, filtra por el set de `personaId` de esa sucursal, logueado (`borrar_vacaciones_sucursal`).
  - Ninguna de las dos acciones toca la nómina (personas) ni la sucursal/fuerza — solo borran períodos de `state.periodos`.
  - Verificado en browser con datos de prueba (5 personas en 4 sucursales incluida CRM, 5 períodos): resumen de chips correcto, grupo CRM aparece con su propia sección, borrar vacaciones de una persona (1 período) y de una sucursal completa (3 períodos de 2 personas) funcionan y quedan en el historial — probado con `window.confirm` mockeado para no colgar la sesión de automatización con el diálogo nativo.
- **Nuevo formato real: rango de fechas en texto libre dentro de la celda "inicio" (2026-09-22, `VACACIONES LA RIOJA 2027.xlsx`):** este plano no trae una fecha sola por columna — trae el rango completo como texto, con separador "AL" y formatos MUY inconsistentes entre filas del mismo archivo: `"18/01 AL 13/02"` (sin año), `"04 AL 23/01"` (inicio sin mes ni año), `"07 AL 19/12/26"` (año de 2 dígitos), `"01/02AL 13/02"` (sin espacio antes de "AL"), y hasta `"PREGUNTAR A COMERCIAL"` (no es una fecha). Con el parser anterior (una fecha por celda) esto importaba 0 filas.
  - **`parseRangoFechas(texto, anioArchivo)`** (nueva, junto a `excelFechaAISO`): separa por `/AL/` como palabra suelta — con lookaround para no matchear "AL" adentro de "COMERCIAL" (precedido por letra) pero sí matchear pegado a un dígito sin espacio como en `"02AL"` (precedido por dígito, no letra). Cada lado se parsea con día/mes/año opcionales (`parseParteFechaRango`); si a un lado le falta el mes, se toma el del otro lado — y si con ese mes "prestado" el inicio queda DESPUÉS del fin (cruce de mes/año, ej. `"21 AL 02/01"` con el mes de enero prestado da 21/01 > 02/01), se corrige tomando el mes ANTERIOR al del fin en vez de asumir el mismo. El año, cuando falta en el texto, se infiere de `anioArchivo` (sacado del nombre del archivo con `/(20\d{2})/`, ej. "...2027.xlsx" → 2027) con la regla real observada en los datos: diciembre cae en `anioArchivo - 1`, el resto de los meses en `anioArchivo` (temporada dic-nov).
  - Se prueba primero (una fecha simple de una sola celda nunca matchea el patrón "X AL Y"); si no da rango, cae al `excelFechaAISO` de siempre — mismo camino que ya andaba bien con Catamarca/Concepción, sin regresión (re-verificado). Cuando SÍ hay rango, el período usa esas fechas tal cual (no se recalcula por feriado/15-días — el dato real ya dice cuándo empieza y termina, aunque no sea lunes o no dure justo 15 días); igual se marca `estado:"revisar"` si el inicio no cae lunes, mismo criterio que siempre.
  - **Fix de paso:** la persona ahora se crea/actualiza en la nómina ANTES de intentar leer la fecha (antes se perdía la fila entera — persona incluida — si la celda no era una fecha reconocible). Nuevo contador `sinFecha` en el toast/log para las filas tipo "PREGUNTAR A COMERCIAL": la persona queda cargada en la nómina igual, simplemente sin período.
  - Verificado en browser con el archivo real (24 filas): 23 personas, 22 períodos, 2 a revisar (uno con rango raro de casi un año completo — `"28/01/26 AL 21/01/27"`, importado tal cual sin intentar "arreglarlo", es una anomalía real del dato), 1 sin fecha reconocible (CAJAL, "PREGUNTAR A COMERCIAL" — quedó en la nómina sin período). Todas las fechas de rango con cruce de mes/año (`"21 AL 02/01"` → 21/12/2026-02/01/2027, `"07 AL 19/12/26"` → dic 2026) resueltas correctamente, verificadas una por una contra el archivo original.

### politicas-sueldos.html (2026-09-24)
- Cuadros nuevos agregados a pedido del usuario, a partir de capturas de tablas reales que fue pasando: escala "Descuentos sobre/PL" por letra (P/A/B/C/D/E) dentro de la sección Outlet-PDO, tabla de la Lista 1000 (antes solo tenía texto, sin tabla), y dos secciones nuevas al final — "Categorías" (rol, cómo se liquida, objetivo de venta/financiado por categoría: Redes, Senior/Junior, Showroom, Cobradores, CRM, Líderes y Gerentes) y "Aclaraciones Importantes" (piso de $8M/producción con la actualización a $10M desde sueldo Octubre 2026, premio sin retención para Redes/Senior que superen $15M).
- **Rediseño visual completo (mismo día, a pedido explícito del usuario):** la línea de diseño se tomó de `comparativa-gerentes.html`/`facturacion-gerentes.html` (topbar plano, cards con borde fino sin sombra pesada, tags/labels en mayúsculas con letter-spacing, tipografía más suave que negro puro) en vez del sistema Notion que usa el resto del panel — decisión consciente del usuario, con la salvedad de que ese archivo de referencia no tenía modo oscuro propio, así que se le agregó una variante dark (`html[data-theme="dark"]`) para no romper el sync de tema compartido (`localStorage.panelTheme`) con el resto de los htmls standalone del repo. El callout "⚠ Importante" de Outlet-PDO se rediseñó sin el borde lateral grueso (`.callout`, ya no `.mini` con `border-left`) — el hook de diseño lo había marcado como anti-patrón ("side-tab") en una revisión anterior de este mismo archivo.
- **Pills de categoría por sección (mismo día):** cada `<h2>` de sección ahora arranca con un pill de color (`.cat-pill.c1`–`.c6`, rotando azul/verde/naranja/rojo/violeta/teal) con el nombre corto de la categoría (ej. "LISTA 3767", "OUTLET / PDO", "PENALIDADES") en vez del tag descriptivo genérico que había antes — pedido puntual del usuario para que se note de un vistazo el cambio de sección al scrollear. Los títulos largos que aportaban info real (Outlet-PDO, Penalidades) conservan el texto completo al lado del pill vía `.h2sub`; los que eran redundantes con el pill se sacaron.
- **Modo edición con contraseña (mismo día):** botón "✎ Editar contenido" en el header abre un modal de acceso (`#editAuthOverlay`, contraseña `379604191919abcdE!` — hardcodeada en el JS, mismo criterio de seguridad ya aceptado por el usuario en el resto del panel, ver notas de `index.html`) que habilita `contenteditable` sobre todos los bloques de texto/tablas/condiciones de la página (`EDIT_IDS`, un id por bloque). Toolbar flotante con Guardar/Salir sin guardar/Restablecer original. **Persistencia: localStorage del navegador únicamente** (decisión explícita del usuario, elegida en vez de armar un backend Apps Script nuevo tipo `NominaBackend.gs`) — los cambios NO se ven desde otra PC/navegador, solo en el que se editó. Pill "Última actualización" en el header se actualiza sola al guardar; pill "Vigencia: Septiembre 2026" queda fija como referencia de la carga original. Verificado en browser: guardar/persistir tras reload, y restablecer vuelve al valor original de los arrays JS.

## Apps Script — pendientes de deploy

- `AIProxy.gs` — pendiente. Ver pasos en `handoff.md`.
- `AgendaBackup.gs` — pendiente. Puede ir como archivo nuevo dentro del proyecto de `LectorAgendas.gs` (variables con prefijo `AGENDA_BACKUP_` a propósito, para no chocar con nada de ese script) o en proyecto aparte. Ver pasos en `handoff.md`.
- `NanoBananaProxy.gs` — pendiente, nuevo (ver sección `generador-placas-ia.html` arriba). Pasos de setup completos en el comment-block del propio archivo.
- ~~`VacacionesBackend.gs`~~ — deployado 2026-09-22 (ver sección `control-vacaciones.html` arriba).
- ~~`NominaBackend.gs`~~ — deployado, confirmado funcionando con datos reales (217 vendedores) al probar la sincronización de `control-vacaciones.html` el 2026-09-22.

## Notas rápidas

- IDs de Drive expuestos en código a propósito (repo público, aceptado por el usuario).
- GitHub Pages tarda ~1-2 min en publicar tras commit.
